DO ONCE
1. Install Node and Mongo db.
2. npm install

DO ALWAYS
3. start the database: mongod --
4. seed the database:  mongo
    load(“[yourpath]/seedscript.js”); 
5. start the server: node index.js
